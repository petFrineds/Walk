apiVersion: apps/v1
kind: Deployment
metadata:
  name: walk-backend
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: walk-backend
  template:
    metadata:
      labels:
        app: walk-backend
    spec:
      containers:
        - name: walk-backend
          image: 811288377093.dkr.ecr.us-west-2.amazonaws.com/walk-backend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: DATE
              value: 'DATE_STRING'